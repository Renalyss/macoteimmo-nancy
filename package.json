{
  "name": "macoteimmonancy",
  "version": "1.0.0",
  "description": "Site d'estimation immobilière MaCoteImmoNancy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo \"Building static site\" && npm run build:static",
    "build:static": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
